---
/* Home — copy pulled from 00-MASTER-OFFER.md (the offer, the guarantees,
   the delivery SOP) rather than invented from scratch. New slots that never
   existed before still carry honest, non-fabricated placeholder marks,
   flagged inline. Light throughout, centered and airy; portfolio is the
   loudest section, but capped short. Purely-decorative sections with no
   real content (the old credibility strip, work wall, atmospheric band,
   composite-visual floating tiles) were cut — they read as unlabeled
   "weird boxes" rather than earning their place on the page. FAQ stays,
   trimmed to the 4 questions that matter, right before the final CTA.
   Signature motion: the large statement-style headings (portfolio heading,
   closing) resolve from near-invisible to full contrast on
   scroll. Respects reduced-motion. */
import BaseLayout from '../layouts/BaseLayout.astro';
import CtaBlock from '../components/CtaBlock.astro';
import FAQ from '../components/FAQ.astro';
import TheProblem from '../components/TheProblem.astro';
import WhatIDo from '../components/WhatIDo.astro';
import WhatGoesIn from '../components/WhatGoesIn.astro';
import { site } from '../data/site.config';
import { homeFaqs } from '../data/faqs';
import { priceCards } from '../data/pricing';

// All 5 broad FAQ questions (each already rolls up several real objections).
const topFaqs = homeFaqs;

// "14 days from today" — computed at build so the date in the final CTA
// stays real on every deploy instead of hardcoding one that goes stale.
const fourteen = new Date(Date.now() + 14 * 24 * 60 * 60 * 1000);
const ordinal = (n: number) => {
  const s = ['th', 'st', 'nd', 'rd'];
  const v = n % 100;
  return `${n}${s[(v - 20) % 10] || s[v] || s[0]}`;
};
const fourteenLabel = `${fourteen.toLocaleDateString('en-US', { month: 'long' })} ${ordinal(fourteen.getDate())}`;
---
<BaseLayout
  title="Conversion-Built Shopify Stores for Consumer Brands"
  description="FlipFix Digital builds custom Shopify storefronts for consumer brands. Live in 14 days or you don't pay, then measured against your old numbers on your own analytics."
>
  <!-- soft gradient wash behind the hero only, fading to flat bg by the fold -->
  <div class="hero-wash" aria-hidden="true"></div>

  <!-- 2. HERO -->
  <section class="hero">
    <div class="container">
      <h1 class="hero__h1"><span>Custom websites</span><span>for consumer brands in 14 days</span></h1>
    </div>
    <div class="container">
      <p class="hero__sub">We don’t stop until it outperforms your old conversion rate.</p>
    </div>

    <!-- 3. VSL — autoplay muted loop under a dark tint (keeps the unmute
         button and progress bar legible over any footage). The circular
         button unmutes and restarts from 0; the bar below is a real
         scrubber. WebM first (smaller, modern browsers), MP4 fallback.
         The poster frame paints instantly so there's never a blank/black
         flash while the (compressed, faststart) video streams in. -->
    <div class="container hero__video">
      <div class="vsl" data-vsl>
        <video
          class="vsl__media"
          autoplay muted playsinline preload="auto"
          disablepictureinpicture controlslist="nodownload noplaybackrate"
          poster="/video/vsl-poster.jpg"
          aria-label="Founder VSL"
        >
          <source src="/video/vsl.webm" type="video/webm" />
          <source src="/video/vsl.mp4" type="video/mp4" />
        </video>
        <div class="vsl__tint" aria-hidden="true"></div>
        <button type="button" class="vsl__replay" data-vsl-replay aria-label="Replay video">
          <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <polygon points="6 3 20 12 6 21 6 3"></polygon>
          </svg>
          <span>Replay</span>
        </button>
        <button type="button" class="vsl__unmute" data-vsl-unmute aria-label="Unmute video">
          <svg class="vsl__unmute-icon vsl__unmute-icon--muted" viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M11 5 6 9H3v6h3l5 4V5Z"></path>
            <line x1="22" y1="9" x2="16" y2="15"></line>
            <line x1="16" y1="9" x2="22" y2="15"></line>
          </svg>
          <svg class="vsl__unmute-icon vsl__unmute-icon--unmuted" viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M11 5 6 9H3v6h3l5 4V5Z"></path>
            <path d="M15.5 8.5a5 5 0 0 1 0 7"></path>
            <path d="M19 5a10 10 0 0 1 0 14"></path>
          </svg>
        </button>
        <div class="vsl__progress">
          <div class="vsl__bar" aria-hidden="true"><div class="vsl__bar-fill" data-vsl-fill></div></div>
          <input type="range" class="vsl__range" min="0" max="100" step="0.1" value="0" data-vsl-range aria-label="Video progress" />
        </div>
      </div>
    </div>

    <!-- CTA BLOCK -->
    <div class="container">
      <div class="hero__cta">
        <CtaBlock event="Hero" />
      </div>
    </div>
  </section>

  <TheProblem />

  <WhatIDo />

  <WhatGoesIn />

  <!-- 6. PRICING (two offers) -->
  <section class="pricing" id="pricing">
    <div class="container pricing__head">
      <p class="pricing__eyebrow mono">The Solution</p>
      <h2 class="pricing__heading">Two <span class="pricing__accent">paths</span>. Zero risk.</h2>
      <p class="pricing__sub">Two different offers, both designed to increase your conversions and revenue.</p>
    </div>
    <div class="container">
      <div class="pricing__grid">
        {priceCards.map((c, i) => (
          <div class:list={['pcard', { 'pcard--featured': c.featured }]} data-reveal style={`transition-delay:${i * 90}ms`}>
            {c.featured && <p class="pcard__tag mono">Most booked</p>}
            <p class="pcard__eyebrow mono">{c.eyebrow}</p>
            <p class="pcard__name mono">{c.name}</p>
            <p class="pcard__desc">{c.description}</p>
            <hr class="pcard__divider" />
            {c.featuresAsChips ? (
              <>
                <p class="pcard__chips-label mono">{c.features[0]}</p>
                <ul class="pcard__chips">
                  {c.features.slice(1).map((f) => <li data-reveal-child>{f}</li>)}
                </ul>
              </>
            ) : (
              <ul class="pcard__features">
                {c.features.map((f) => (
                  <li data-reveal-child>
                    <svg class="pcard__check" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="20 6 9 17 4 12"></polyline></svg>
                    {f}
                  </li>
                ))}
              </ul>
            )}
            <a class="btn btn--primary btn--pill pcard__cta" href={`${site.cta.href}?from=${encodeURIComponent(c.event)}`} data-event={c.event}><span class="btn__label">{c.cta}</span></a>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- FAQ (top 5 questions) -->
  <section class="faq-section">
    <div class="container">
      <div class="faq-section__head">
        <p class="eyebrow-num mono eyebrow-num--center">FAQ</p>
        <h2 class="faq-section__heading stmt">Common Questions</h2>
      </div>
      <div class="faq-section__list"><FAQ items={topFaqs} /></div>
      <p class="faq-section__more" aria-hidden="true">↓</p>
    </div>
  </section>

  <!-- 11. FINAL CTA (dark) — right before the footer. The page's thesis
       statement, made the loudest visual on the page: an oversized
       count-up on "14" plus an animated today-to-launch timeline. -->
  <section class="closing section--dark">
    <div class="container closing__inner">
      <h2 class="closing__h stmt">
        <span class="closing__h-days"><span data-count-to="14">14</span> days</span> from today is {fourteenLabel}.
      </h2>
      <div class="countdown-track" data-reveal aria-hidden="true">
        <span class="countdown-track__point">
          <span class="countdown-track__dot"></span>
          <span class="mono">Today</span>
        </span>
        <span class="countdown-track__bar"><span class="countdown-track__fill"></span></span>
        <span class="countdown-track__point">
          <span class="countdown-track__dot countdown-track__dot--end"></span>
          <span class="mono">{fourteenLabel}</span>
        </span>
      </div>
      <p>You’re already paying for every visitor who lands on your site, whether they buy or leave. In 14 days, your conversion rate goes up.</p>
      <p>Tap the button, fill out the short form, pick a time.</p>
      <p class="closing__alt">Prefer email? <a href={`mailto:${site.email}`}>{site.email}</a></p>
      <div class="closing__cta">
        <CtaBlock event="Closing" />
      </div>
      <p class="closing__sig">{site.founder}, {site.brand}</p>
    </div>
  </section>
</BaseLayout>

<script>
  // ---- Signature motion: statement-style headings resolve on scroll ----
  (function () {
    var stmts = Array.prototype.slice.call(document.querySelectorAll('.stmt'));
    if (!stmts.length) return;
    var reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    var litAll = function () { stmts.forEach(function (s) { s.classList.add('is-lit'); }); };
    if (reduce || !('IntersectionObserver' in window)) { litAll(); return; }
    var io = new IntersectionObserver(function (entries) {
      entries.forEach(function (e) { if (e.isIntersecting) { e.target.classList.add('is-lit'); io.unobserve(e.target); } });
    }, { threshold: 0, rootMargin: '0px 0px -12% 0px' });
    stmts.forEach(function (s) { io.observe(s); });
    // failsafe: never leave a heading stuck faint
    window.addEventListener('load', function () { setTimeout(litAll, 700); });
  })();

  // ---- VSL: unmute control + scrubbable progress bar ----
  (function () {
    var vsl = document.querySelector('[data-vsl]');
    if (!vsl) return;
    var media = vsl.querySelector('.vsl__media');
    var btn = vsl.querySelector('[data-vsl-unmute]');
    var range = vsl.querySelector('[data-vsl-range]');
    var fill = vsl.querySelector('[data-vsl-fill]');
    var replayBtn = vsl.querySelector('[data-vsl-replay]');
    if (!media || !btn) return;
    // Autoplay is handled natively via the `autoplay` attribute; Safari/iOS
    // in particular can still need a nudge once metadata is ready.
    media.addEventListener('loadedmetadata', function () { media.play().catch(function () {}); });

    // Plays once, then shows a Replay overlay instead of looping silently.
    media.addEventListener('ended', function () { vsl.classList.add('is-ended'); });
    if (replayBtn) {
      replayBtn.addEventListener('click', function () {
        vsl.classList.remove('is-ended');
        media.currentTime = 0;
        media.play().catch(function () {});
      });
    }

    // ---- Retention: beacon the furthest second actually watched to
    // /api/vsl, which feeds the retention chart on /pulse. It autoplays
    // muted, so raw playback time alone can't tell you whether anyone's
    // actually looking — so playback is paused (and a beacon fires) the
    // moment the tab is hidden or the player scrolls out of view, and only
    // resumes once it's back in view. A backgrounded/ignored autoplay can't
    // rack up phantom seconds; "clicking off" genuinely counts as leaving.
    // Each session only ever reports its watermark — the server tops up the
    // histogram by the delta, so a later beacon never double-counts.
    (function () {
      var sessionId = 'v_' + Math.random().toString(36).slice(2) + Date.now().toString(36);
      var maxSecond = 0;
      var lastSent = 0;
      var autoPaused = false;
      var inView = true;

      function beacon(done) {
        if (maxSecond <= lastSent && !done) return;
        lastSent = maxSecond;
        var payload = JSON.stringify({ id: sessionId, second: maxSecond, duration: Math.round(media.duration) || 0 });
        try {
          if (navigator.sendBeacon) {
            navigator.sendBeacon('/api/vsl', new Blob([payload], { type: 'application/json' }));
          } else {
            fetch('/api/vsl', { method: 'POST', body: payload, headers: { 'Content-Type': 'application/json' }, keepalive: true });
          }
        } catch (e) {}
      }

      media.addEventListener('timeupdate', function () {
        var sec = Math.floor(media.currentTime);
        if (sec > maxSecond) maxSecond = sec;
      });
      media.addEventListener('ended', function () { beacon(true); });
      window.addEventListener('pagehide', function () { beacon(false); });
      document.addEventListener('visibilitychange', function () {
        if (document.hidden) {
          beacon(false);
          if (!media.paused) { media.pause(); autoPaused = true; }
        } else if (autoPaused && inView) {
          media.play().catch(function () {});
          autoPaused = false;
        }
      });

      if ('IntersectionObserver' in window) {
        new IntersectionObserver(function (entries) {
          inView = entries[0].isIntersecting;
          if (!inView) {
            if (!media.paused) { media.pause(); autoPaused = true; beacon(false); }
          } else if (autoPaused && !document.hidden) {
            media.play().catch(function () {});
            autoPaused = false;
          }
        }, { threshold: 0.4 }).observe(vsl);
      }
    })();

    btn.addEventListener('click', function () {
      var wasMuted = media.muted;
      media.muted = !wasMuted;
      btn.setAttribute('aria-label', media.muted ? 'Unmute video' : 'Mute video');
      vsl.classList.toggle('is-unmuted', !media.muted);
      // once the visitor has touched sound at all, the button shrinks into a
      // corner toggle for good — no snapping back to the big centered
      // overlay just because they muted again
      vsl.classList.add('is-engaged');
      // unmuting is a deliberate "watch it properly" action — start over
      if (wasMuted) media.currentTime = 0;
      if (media.paused) media.play().catch(function () {});
    });

    if (range && fill) {
      var scrubbing = false;
      var setFromMedia = function () {
        if (scrubbing || !media.duration) return;
        var pct = (media.currentTime / media.duration) * 100;
        range.value = String(pct);
        fill.style.width = pct + '%';
      };
      var setFromRange = function () {
        fill.style.width = range.value + '%';
        if (media.duration) media.currentTime = (parseFloat(range.value) / 100) * media.duration;
      };
      media.addEventListener('timeupdate', setFromMedia);
      media.addEventListener('loadedmetadata', setFromMedia);
      range.addEventListener('pointerdown', function () { scrubbing = true; });
      range.addEventListener('input', setFromRange);
      range.addEventListener('pointerup', function () { scrubbing = false; });
    }
  })();

</script>

<style>
  /* ---------- hero load sequence (runs once on arrival, no re-trigger) ----------
     Logo (Nav.astro, self-contained) fades first, then the headline lines
     stagger up, subhead follows, CTA + the 3-step process strip stagger in
     last. Delays overlap rather than chain off full durations so the whole
     thing lands under ~1s instead of stacking to a slow crawl. */
  @keyframes hero-rise { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: none; } }
  .js .hero__h1 span { opacity: 0; animation: hero-rise 500ms var(--ease-out) forwards; }
  .js .hero__h1 span:nth-child(1) { animation-delay: 100ms; }
  .js .hero__h1 span:nth-child(2) { animation-delay: 160ms; }
  .js .hero__sub { opacity: 0; animation: hero-rise 450ms var(--ease-out) 260ms forwards; }
  .js .hero__cta :global(.cta-block) { opacity: 0; animation: hero-rise 450ms var(--ease-out) 360ms forwards; }
  .js .hero__cta :global(.cta-repeat__process > span:nth-child(1)),
  .js .hero__cta :global(.cta-repeat__process > span:nth-child(3)),
  .js .hero__cta :global(.cta-repeat__process > span:nth-child(5)) {
    opacity: 0; animation: hero-rise 400ms var(--ease-out) forwards;
  }
  .js .hero__cta :global(.cta-repeat__process > span:nth-child(1)) { animation-delay: 440ms; }
  .js .hero__cta :global(.cta-repeat__process > span:nth-child(3)) { animation-delay: 520ms; }
  .js .hero__cta :global(.cta-repeat__process > span:nth-child(5)) { animation-delay: 600ms; }

  /* ---------- soft hero wash (behind hero only) ---------- */
  .hero-wash {
    position: absolute; inset: 0 0 auto 0; height: 96vh; z-index: 0; pointer-events: none;
    background:
      radial-gradient(80% 60% at 50% 12%, color-mix(in srgb, var(--accent) 12%, transparent) 0%, transparent 60%),
      linear-gradient(to bottom, color-mix(in srgb, var(--accent) 5%, var(--bg)) 0%, var(--bg) 62%);
  }


  /* ---------- 2. HERO ---------- */
  /* nav is a regular in-flow bar now (not a floating overlay), so the hero
     no longer needs to reserve space to clear it. Spacing matches the
     reference done-for-you hero: space-12 top, space-6 to the video,
     space-5 to the CTA. */
  .hero { position: relative; z-index: 1; padding-top: var(--space-12); padding-bottom: var(--space-10); text-align: center; }
  /* inherits the global h1 (uppercase Archivo display, var(--fs-h1)) — only
     margin/max-width are overridden, same as the reference hero. ch scales
     with the font itself, so "Custom converting websites" stays on one
     line at any viewport instead of wrapping mid-phrase. */
  .hero__h1 { max-width: 34ch; margin-inline: auto; font-size: clamp(2.25rem, 4.4vw, 3.75rem); }
  .hero__h1 span { display: block; }
  .hero__h1 span:first-child { white-space: nowrap; }
  .hero__sub { margin: var(--space-5) auto 0; max-width: 44ch; font-family: var(--font-serif); font-style: italic; font-size: var(--fs-lede); color: var(--ink-soft); }
  .hero__video { margin-top: var(--space-6); }
  .hero__cta { margin-top: var(--space-5); }

  /* ---------- 3. VSL ---------- */
  .vsl {
    position: relative; width: 100%; max-width: 68rem; aspect-ratio: 16 / 9;
    margin-inline: auto; border-radius: 16px; overflow: hidden;
    background: #0f120e; box-shadow: 0 40px 80px -40px rgba(26, 24, 20, 0.5);
    display: flex; align-items: center; justify-content: center;
  }
  .vsl__media { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; }
  /* darkens the footage so the unmute button and progress bar stay legible
     over any frame while muted — once the visitor unmutes, they've already
     engaged, so fade it out and let the footage read normally */
  .vsl__tint {
    position: absolute; inset: 0; pointer-events: none;
    background: linear-gradient(180deg, rgba(0,0,0,0.3) 0%, rgba(0,0,0,0.12) 28%, rgba(0,0,0,0.12) 60%, rgba(0,0,0,0.5) 100%);
    opacity: 1; transition: opacity var(--duration-base) var(--ease-out);
  }
  .vsl.is-unmuted .vsl__tint { opacity: 0; }
  .vsl__unmute {
    position: absolute; z-index: 2; width: 72px; height: 72px; border-radius: 50%;
    border: 2px solid var(--dark-ink); background: color-mix(in srgb, var(--dark-ink) 12%, transparent);
    color: var(--dark-ink); cursor: pointer;
    display: flex; align-items: center; justify-content: center;
    transition: width var(--duration-base) var(--ease-out), height var(--duration-base) var(--ease-out),
                top var(--duration-base) var(--ease-out), right var(--duration-base) var(--ease-out),
                transform var(--duration-fast) var(--ease-out), background var(--duration-fast) var(--ease-out);
  }
  .vsl__unmute:hover { transform: scale(1.06); background: color-mix(in srgb, var(--dark-ink) 22%, transparent); }
  /* only one of the two icons is ever shown, matching the actual mute state
     (never a plain "play" glyph — this button is sound-only, the video is
     always already playing) */
  .vsl__unmute-icon { display: none; }
  .vsl__unmute-icon--muted { display: block; }
  .vsl.is-unmuted .vsl__unmute-icon--muted { display: none; }
  .vsl.is-unmuted .vsl__unmute-icon--unmuted { display: block; }
  /* after the first interaction, get out of the way: shrink into a corner
     toggle instead of staying a giant circle over the footage */
  .vsl.is-engaged .vsl__unmute {
    width: 40px; height: 40px; top: var(--space-4); right: var(--space-4); left: auto; bottom: auto; transform: none;
  }
  .vsl.is-engaged .vsl__unmute:hover { transform: scale(1.06); }
  .vsl.is-engaged .vsl__unmute-icon { width: 16px; height: 16px; }

  /* plays once, then this replaces the unmute button entirely — no silent
     loop, no ambiguity about whether it's still going */
  .vsl__replay {
    position: absolute; z-index: 3; display: none; align-items: center; gap: var(--space-2);
    padding: var(--space-3) var(--space-5); border-radius: 999px;
    border: 2px solid var(--dark-ink); background: color-mix(in srgb, var(--dark-ink) 55%, transparent);
    color: var(--dark-ink); cursor: pointer;
    font-family: var(--font-mono); font-size: var(--fs-caption); font-weight: var(--fw-body-semi);
    text-transform: uppercase; letter-spacing: var(--tracking-caps);
    transition: transform var(--duration-fast) var(--ease-out), background var(--duration-fast) var(--ease-out);
  }
  .vsl__replay:hover { transform: scale(1.05); background: color-mix(in srgb, var(--dark-ink) 70%, transparent); }
  .vsl.is-ended .vsl__replay { display: flex; }
  .vsl.is-ended .vsl__unmute { display: none; }
  .vsl.is-ended .vsl__tint { opacity: 1; }

  /* scrubbable progress bar: a native range input (full touch target) laid
     over a slim visual track + fill, same "invisible functional control
     over a custom visual" technique as the before/after slider */
  .vsl__progress { position: absolute; left: 0; right: 0; bottom: 0; z-index: 2; padding: var(--space-4); padding-top: var(--space-6); }
  .vsl__bar { position: relative; width: 100%; height: 4px; border-radius: 999px; background: rgba(255,255,255,0.3); overflow: hidden; }
  .vsl__bar-fill { position: absolute; inset: 0 auto 0 0; width: 0%; background: var(--accent-strong); border-radius: 999px; }
  .vsl__range {
    position: absolute; inset: 0; width: 100%; height: 100%; margin: 0;
    appearance: none; -webkit-appearance: none; background: transparent; cursor: pointer;
  }
  .vsl__range::-webkit-slider-thumb {
    appearance: none; -webkit-appearance: none; width: 14px; height: 14px; border-radius: 50%;
    background: var(--accent-strong); box-shadow: 0 0 0 3px rgba(0,0,0,0.35); cursor: pointer;
  }
  .vsl__range::-moz-range-thumb { width: 14px; height: 14px; border-radius: 50%; background: var(--accent-strong); border: 0; box-shadow: 0 0 0 3px rgba(0,0,0,0.35); cursor: pointer; }
  .vsl__range::-moz-range-track { background: transparent; }

  /* ---------- signature statement motion ---------- */
  .stmt { transition: opacity 800ms var(--ease-out), color 800ms var(--ease-out); }
  .js .stmt { opacity: 0.35; }
  .js .stmt.is-lit { opacity: 1; }

  /* ---------- 6. PRICING (two offers — made to stand out, pricing-inspo) ---------- */
  .pricing { padding-block: clamp(2.5rem, 7vh, 5rem); }
  .pricing__head { text-align: center; margin-bottom: var(--space-12); }
  .pricing__eyebrow { display: flex; align-items: center; justify-content: center; gap: var(--space-4); color: var(--ink-50); }
  .pricing__eyebrow::before, .pricing__eyebrow::after { content: ""; width: 2.5rem; height: var(--rule-width); background: var(--line); }
  .pricing__heading { margin-top: var(--space-4); font-size: clamp(2.25rem, 5.5vw, 3.75rem); }
  .pricing__accent { color: var(--accent-deep); }
  .pricing__sub { margin-top: var(--space-4); font-size: var(--fs-lede); color: var(--ink-soft); max-width: 42ch; margin-inline: auto; }
  .pricing__grid { display: grid; grid-template-columns: 1fr; gap: var(--space-6); max-width: 64rem; margin-inline: auto; }
  @media (min-width: 44em) { .pricing__grid { grid-template-columns: 1fr 1fr; gap: var(--space-8); } }
  /* .pcard* itself is shared (components.css) — reused as-is on /apply's
     "which option" question so the two never drift apart. */

  /* ---------- FAQ ---------- */
  .faq-section { padding-block: clamp(2.5rem, 7vh, 5rem); }
  .faq-section__head { text-align: center; }
  .faq-section__heading { margin-top: var(--space-4); margin-inline: auto; font-size: clamp(2rem, 5vw, 3.25rem); }
  .faq-section__list { margin-top: var(--space-8); }
  .faq-section__more { margin-top: var(--space-8); text-align: center; font-size: 1.5rem; color: var(--ink-50); animation: faq-more-bounce 1800ms ease-in-out infinite; }
  @keyframes faq-more-bounce { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(6px); } }
  @media (prefers-reduced-motion: reduce) { .faq-section__more { animation: none; } }

  /* ---------- 11. FINAL CTA (dark) ---------- */
  .closing { padding-block: clamp(3rem, 10vh, 6rem); }
  .closing__inner { text-align: center; }
  .closing__h { font-size: clamp(2rem, 5.5vw, 4rem); max-width: 26ch; margin-inline: auto; }

  /* the page's thesis statement, made the loudest element on the page:
     "14 days" runs bigger than the rest of the headline, in brass, with a
     slow persistent glow; a below timeline visually marks today → launch */
  .closing__h-days {
    display: inline-block; font-size: 1.5em; line-height: 0.9; color: var(--accent-strong);
    animation: closing-glow 3s var(--ease-io) infinite;
  }
  .closing__h-days > span:first-child { font-variant-numeric: tabular-nums; }
  @keyframes closing-glow {
    0%, 100% { text-shadow: 0 0 18px color-mix(in srgb, var(--accent-strong) 0%, transparent); }
    50%      { text-shadow: 0 0 18px color-mix(in srgb, var(--accent-strong) 55%, transparent); }
  }

  .countdown-track { margin: var(--space-6) auto 0; max-width: 26rem; display: flex; align-items: flex-start; gap: var(--space-3); }
  .countdown-track__point { display: flex; flex-direction: column; align-items: center; gap: var(--space-2); flex-shrink: 0; width: 5.5rem; }
  .countdown-track__dot { width: 10px; height: 10px; border-radius: 50%; background: var(--dark-line); box-shadow: 0 0 0 4px color-mix(in srgb, var(--dark-ink) 8%, transparent); }
  .countdown-track__dot--end { background: var(--accent-strong); box-shadow: 0 0 0 4px color-mix(in srgb, var(--accent-strong) 22%, transparent); }
  .countdown-track__point .mono { font-size: var(--fs-small); text-transform: uppercase; letter-spacing: var(--tracking-caps); color: var(--dark-soft); white-space: nowrap; }
  .countdown-track__bar { position: relative; flex: 1; height: 2px; margin-top: 4px; background: var(--dark-line); border-radius: 2px; overflow: hidden; }
  .countdown-track__fill {
    position: absolute; inset: 0; background: var(--accent-strong); border-radius: 2px;
    transform-origin: left; transform: scaleX(0);
    transition: transform 1100ms var(--ease-out) 250ms;
  }
  .js [data-reveal].is-in .countdown-track__fill { transform: scaleX(1); }

  .closing__inner p { margin-top: var(--space-4); font-family: var(--font-serif); font-size: var(--fs-bodyL); line-height: 1.65; color: var(--dark-soft); max-width: 56ch; margin-inline: auto; }
  .closing__alt { font-size: var(--fs-body) !important; }
  .closing__alt a { color: var(--dark-ink); font-weight: var(--fw-body-bold); text-decoration: underline; text-underline-offset: 3px; }
  .closing__alt a:hover { color: var(--accent-strong); }
  .closing__cta { margin-top: var(--space-8); }
  .closing__sig { margin-top: var(--space-8) !important; font-family: var(--font-display) !important; font-size: var(--fs-h4) !important; color: var(--dark-ink) !important; }



</style>
